# --- secrets : ne JAMAIS versionner ---
data/raw/tomtom_key.txt
**/tomtom_key.txt
token_cron.txt
**/token_cron.txt
**/*token*.txt
**/*secret*
*.key
*.pem

# --- donnees brutes telechargees (volumineuses, regenerables via scripts/download_*.py) ---
data/raw/
data/interim/
cache/

# data/rt/ (pollers GTFS-RT + autoroutes) et data/output/ SONT versionnes
# le fichier de test local des autoroutes ne doit pas partir avec la cle en dur : rien a exclure,
# le script lit la cle depuis l'env (Actions) ou data/raw/ (gitignore) -- jamais ecrite dans le CSV

# --- python ---
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# --- OS ---
Thumbs.db
.DS_Store
